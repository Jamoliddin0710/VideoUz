<div class="p-4">
    <h2> Categoires </h2>
    <button type="button" class="btn btn-primary actions" onclick="createnewcategory()">Create</button>
    <table class="table table-striped table-bordered mt-2">
        <thead>
        <tr>
            <td>Id</td>
            <td>Name</td>
            <td>Actions</td>
        </tr>
        </thead>
        <tbody id="categories">
        
        </tbody>
    </table>
</div>

@section Scripts
{
    <script>
        $(function (){
            getCategories();
        })
        
        function getCategories(){
            $.ajax({
                uri : "@Url.Action("GetAllCategories")",
                type:"GET",
                success: function (data){
                    console.log(data);
                    @*if (data.issucces){
                        $('categories').add(data.result);
                    }*@
                },
                error: function (xhr, ajaxOptions, thrownError) {
                alert(xhr.status);
                alert(thrownError);
            }
            })
        }
    </script>
   
}
